server:
  port: 8083

spring:
  application:
    name: address
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  datasource:
    url: X
    username: X
    password: X
